# =====================================================
# VARIABLES DE ENTORNO - FRONTEND
# =====================================================
# Solo variables públicas para el cliente
# =====================================================

# URL Base de la API (Supabase Edge Functions)
# En desarrollo local de Supabase
NEXT_PUBLIC_API_BASE_URL=http://localhost:54321/functions/v1

# En producción (reemplazar con tu proyecto real)
# NEXT_PUBLIC_API_BASE_URL=https://your-project-ref.supabase.co/functions/v1

# API Key pública (anon key) - SOLO PARA HEADERS SI ES NECESARIO
# Por defecto, las Edge Functions no requieren autenticación para testing
# Pero si configuras auth, aquí iría la anon key
# NEXT_PUBLIC_API_KEY=eyJhbGc...

# Configuración de la aplicación
NEXT_PUBLIC_APP_NAME="Sistema Educativo Adaptativo"
NEXT_PUBLIC_APP_VERSION=1.0.0

# =====================================================
# NOTA IMPORTANTE:
# =====================================================
# El frontend NO tiene dependencia directa de Supabase
# Solo hace llamadas HTTP a las Edge Functions
# Supabase es el backend completamente separado
# =====================================================
